extends layout

block content
    each item in videos
        div
            span #{item.title}
            div
                iframe(width="420", height="315", src="#{item.url}", frameborder="0", allowfullscreen)